{
  "name": "@brinkninja/types",
  "version": "0.14.1",
  "description": "TypeScript types for Brink",
  "license": "GPL-3.0-or-later",
  "main": "dist/Types.js",
  "types": "dist/Types.d.ts",
  "scripts": {
    "build": "tsc",
    "release": "standard-version --skip.changelog --skip.commit --skip.tag"
  },
  "dependencies": {
    "standard-version": "^9.5.0",
    "typescript": "^5.0.4"
  }
}
